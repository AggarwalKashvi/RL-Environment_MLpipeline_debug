name: "ml-pipeline-debugger"
version: "1.0.0"
description: "A real-world simulation environment where an AI agent diagnoses and fixes bugs across stages of a broken ML pipeline."
entrypoint: "server.app:app"